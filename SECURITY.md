# Security Model

## 受保护资产

- 本机文件、环境变量和网络凭据
- 用户上传的数据
- 应用进程的 CPU、内存和可用性
- 分析结果的业务口径与完整性

## 信任边界

用户问题、CSV 内容和模型输出都视为不可信输入。指标配置和仓库内参考代码视为受信任版本化资产。

## 已实施控制

### SQL 路径

- SQLGlot AST 解析
- 单语句、SELECT-only
- 只允许访问 `source_data`
- 禁止外部 CSV/JSON/Parquet、HTTP 与数据库扫描函数
- 自动增加 200 行 LIMIT
- DuckDB 内存连接，用后关闭

### Python 路径

- 禁止 Import、循环、函数、类、异常控制、上下文管理器和异步语法
- 禁止文件、网络、反射、动态执行和双下划线属性
- 禁止 `plt.show()` / `plt.pause()` 等阻塞调用
- 代码长度、AST 节点数和超大常量限制
- 受限 builtins，DataFrame 副本执行

### 输入与质量

- CSV 最大 50 MB、100 万行
- 编码与分隔符检测
- 重复字段名拒绝
- 缺失、重复、字段基数和分布画像
- 40 道功能题与 12 道安全回归题

## 不保证的边界

当前 Python 执行仍发生在应用进程内。AST 黑名单无法证明任意 Python 代码安全，也无法强制 CPU/内存/系统调用级限制。因此本项目适合本地作品集和受信任团队原型，不应直接暴露给不可信公网用户。

生产部署应使用一次一容器或微虚拟机：只读根文件系统、无网络、非 root、丢弃 capabilities、`no-new-privileges`、CPU/内存/PID/超时限制，并只通过结构化输入输出交换数据。

## 漏洞报告

请不要在公开 Issue 中提交真实数据、凭据或可利用细节。通过仓库维护者的私有联系方式报告，并附最小复现。

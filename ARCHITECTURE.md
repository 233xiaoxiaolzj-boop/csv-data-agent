# Architecture Decision Record

## 目标

系统要解决的不是“让模型写出一段能跑的代码”，而是让自然语言分析具备明确口径、最小执行权限、可观察性和可量化质量。

## 为什么采用双引擎

常规筛选、分组、聚合和时间查询适合 SQL：表达更短、权限更容易约束、结果更容易审计。绘图、相关性和开放式统计仍保留 pandas，因为这些任务用 SQL 表达会显著降低可读性。

自动路由基于明确任务词，而不是再调用一次模型：这降低延迟和不确定性。用户也可以在界面强制选择引擎，便于调试和对比。

## 语义层

业务指标存放在 `config/metrics.json`，与应用代码和 Prompt 分离。每个指标包含：

- 稳定名称、中文标签与同义词
- 业务定义和参考公式
- 所需字段

运行时只把与问题匹配、且当前数据具备所需字段的口径加入上下文。这解决了小模型常见的“销量/销售额”“退货订单数/退货率”混淆，也避免一次注入全部指标造成上下文噪声。

## SQL 执行路径

1. Ollama 生成一条 DuckDB SQL。
2. SQLGlot 解析 AST；拒绝多语句、非 SELECT、非白名单表和外部读取函数。
3. 无 LIMIT 时追加 200 行限制。
4. DuckDB 只在内存连接中注册当前 DataFrame，执行后关闭连接。
5. 返回结果表、规范化 SQL、生成尝试和执行延迟。

## Python 执行路径

1. Ollama 生成 pandas/Matplotlib 代码。
2. AST 拒绝导入、循环、函数、类、文件/网络、动态执行、双下划线、阻塞式绘图和复杂代码。
3. 检查必须存在 `result`，且 result 不能是 Figure/Axes/Plot 对象。
4. 首轮失败时携带原问题、相关指标和错误修复一次。
5. 在仅暴露 df/pd/np/plt 与安全内置函数的命名空间执行 DataFrame 副本。

## 可观察性

每次分析展示引擎、生成尝试、自动修复、公式回退、生成 SQL/代码和延迟。评测 JSON 额外保留逐题问题、生成代码和失败原因，便于回归分析。

## 已知取舍

- 基于关键词的路由简单、透明，但对隐含统计意图识别有限。
- SQLGlot + 表白名单能显著缩小攻击面，但不是数据库权限系统的替代品。
- Python AST 校验只能做应用层防护，无法替代容器/微虚拟机隔离。
- 当前数据是单表 CSV；多表语义、连接路径和血缘属于下一阶段。
